"""
core/systems/journal -- Permanent Objects journal-planner subsystem.

The original day-planner at the heart of the project. Each entry's
free-text raw_note is canon (her voice, never altered). A derived
personal lexicon accumulates novel terms with their contexts; the
game queries the lexicon (never raw_note) for personalization signal.

Architecture: see memory `design_lexicon_architecture`.
Phase plan:    see memory `project_permanent_objects_journal`.
"""
