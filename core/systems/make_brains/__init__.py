"""Built-in make-brain handlers.

Each module in this package implements one make-brain instance
(`ping_pong`, future `archery`, future puzzle modules). They share the
universal substrate from `core.systems.make_brain_registry` +
`core.vault` (profiles, runs).

See `.claude/feature/feat_make-brain-ping-pong.md`.
"""
